该README文件为开发时给ai作为提示词的用处。

我要写一个健康记录软件
该软件为离线、本地运行软件
软件的平台为：Android apk
选用的IDE是：Android studio
系统是：windows10
软件风格：Material Design 3 (MD3)
软件的核心功能是：记录和数据分析报表
需要记录的内容有：食品的大三营养素、食品的热量、体重、体脂率、肌肉量、三围、入睡\起床时间
数据分析报表需要的图表有：堆叠柱状图、并且可选择叠加一条折线来观察总摄入热量、或者单个营养素摄入的变化（三大营养素）、平滑曲线的折线图（体重、体脂率、肌肉量、三围）、范围条形图（入睡\起床时间以及睡眠时长）
数据分析时单位周期有：天、周、月、年

特殊功能：
1、身体数据（体重、体脂率、肌肉量、三围）在以一周为一个单位点（折线图上的每一点代表一个周）时，可以选取这一周的该数据的中位数
2、用户可以自定义饮食计划（例如每天每顿吃什么、吃多少），用户可以以一顿、一天、一周、一月为单位，添加每顿的食物内容，保存成为一个饮食计划。预先设定好的饮食计划，会在每日摄入中提前显示，并且在每行食物前会有一个可以check的小方块，一旦摄入，将小方框check，即视为添加到今日的食物摄入之内，若过了当日没有check，则视为为摄入，该项食物自动忽略消失。
3、一个食物如果花费n天吃完，则在记录时，选好摄入的n个日期后，将总热量平分到每天的摄入。
4、软件数据由用户自行导出备份导入恢复，备份包内只含有文本数据
5、软件高度自定义，报表高度自由化，用户可自行选择是否展示某个报表，以及以什么周期来展示
6、由于是本地运行的离线免费软件，所以我们需要先行爬取权威食品数据库的食品营养表数据，内置在软件内部，主要针对平时没有特殊标明食品营养表的食物（类似菜市场买的菜）

软件细节：
1、可以设置目标体重、体脂率、摄入量（热量或者三大营养素）。目标身体数据需要在折线图上以虚线显示。
2、睡眠的范围条形图下面需要有一行字，显示在一个周期内，平均入睡\起床时间，该周期与睡眠的范围条形图的周期一致。
3、软件有两个页面，首页为当日数据（摄入、身体、睡眠）在左下角为一个圆形，内有+号，点开后展开三个选项（摄入、身体数据、睡眠）点击其中一个选项进入二级页面（全屏显示）进行具体数据记录；第二个页面为报表页面，报表页面右上角可导出导入数据
4、食物以简笔画或者图标的形式显示，无需具体图片，具体食物内容不用细化（例如肉类都用一种图标即可，节省内存占用）
5、首页顶部横幅显示当日可摄入的热量，以半圆形式显示，并且如果指定的摄入量大于基础代谢，则在半圆上标明基础代谢的位置
6、首次使用软件，需要填写计算所需的身体数据，然后根据数据自动计算出BMR和TDEE，计算出的两个数据，后续在首页左上角用户图标点击即可查看

注意：
1、每次输出都先输出copy，然后再输出内容
2、你指导我在Android studio里的每一步行动
3、在说studio里每个选项时，中英文同时输出（例如：class 类）
4、每写出/修改一个功能，就应该提交一次git
5、停止输出时，必须发出提示音

最终：
1、部署到GitHub，并且告诉我须知事项
2、总结软件所有功能，写一个GitHub页面的readme
3、测试所有功能

================================================================================
                                     项目进度记录
================================================================================

【已完成功能概览】

一、数据层 (Data Layer)
  ├── Entity 实体类 (7个)
  │   ├── FoodEntity - 食品营养数据
  │   ├── IntakeRecordEntity - 摄入记录（支持多天分摊）
  │   ├── BodyRecordEntity - 身体数据（体重/体脂/肌肉/三围）
  │   ├── SleepRecordEntity - 睡眠记录
  │   ├── MealPlanEntity - 饮食计划
  │   ├── MealPlanItemEntity - 饮食计划项目
  │   └── UserSettingsEntity - 用户设置
  ├── DAO 数据访问对象 (7个)
  │   └── 完整的 CRUD 操作和查询方法
  ├── Database
  │   └── HealthTrackerDatabase - Room 数据库配置
  └── Repository 仓库层 (6个)
      └── 数据访问封装

二、依赖注入 (Hilt)
  └── DatabaseModule - 数据库和 DAO 提供

三、UI 层 (Jetpack Compose)
  ├── 导航
  │   ├── Screen.kt - 路由定义 (11个页面)
  │   └── HealthTrackerNavGraph.kt - 导航图
  ├── 首页 (HomeScreen)
  │   ├── 热量半圆进度显示
  │   ├── 营养素摘要卡片
  │   ├── 今日摄入列表
  │   ├── 身体数据卡片
  │   ├── 睡眠数据卡片
  │   └── 多功能 FAB (展开式添加按钮)
  ├── 报表页 (ReportsScreen) - 基础结构
  ├── 记录页面
  │   ├── AddIntakeScreen - 添加摄入
  │   ├── AddBodyDataScreen - 添加身体数据
  │   └── AddSleepScreen - 添加睡眠
  └── 设置页面
      ├── SettingsScreen - 设置主页
      ├── UserProfileScreen - 用户资料
      ├── MealPlansScreen - 饮食计划
      ├── FoodManagerScreen - 食品管理
      ├── DataExportScreen - 数据导出
      └── DataImportScreen - 数据导入

四、工具类
  ├── DateTimeUtils - 日期时间处理
  └── HealthCalculator - 健康计算 (BMR/TDEE等)

五、内置数据
  └── DefaultFoods - 20种常见食品营养数据

六、主题
  └── Material Design 3 主题配置

================================================================================
                                   食物数据导入格式说明
================================================================================

软件支持从 JSON 或 CSV 文件导入食物营养数据，导入入口：设置 → 导入食物数据

【JSON 格式】
───────────────────────────────────────────────────────────────────────────────
{
  "dataSource": "数据来源说明（可选）",
  "note": "其他备注信息（可选）",
  "foods": [
    {
      "name": "食物名称",
      "category": "分类",
      "calories": 热量数值,
      "carbohydrates": 碳水化合物数值,
      "protein": 蛋白质数值,
      "fat": 脂肪数值,
      "emoji": "图标"
    }
  ]
}
───────────────────────────────────────────────────────────────────────────────

【JSON 示例】
───────────────────────────────────────────────────────────────────────────────
{
  "dataSource": "USDA FoodData Central",
  "foods": [
    {
      "name": "大米",
      "category": "主食",
      "calories": 346,
      "carbohydrates": 77.2,
      "protein": 7.4,
      "fat": 0.8,
      "emoji": "🍚"
    },
    {
      "name": "鸡肉",
      "category": "肉类",
      "calories": 156,
      "carbohydrates": 0.0,
      "protein": 23.9,
      "fat": 6.0,
      "emoji": "🥩"
    }
  ]
}
───────────────────────────────────────────────────────────────────────────────

【CSV 格式】
───────────────────────────────────────────────────────────────────────────────
name,category,calories,carbohydrates,protein,fat,emoji
大米,主食,346,77.2,7.4,0.8,🍚
鸡肉,肉类,156,0.0,23.9,6.0,🥩
───────────────────────────────────────────────────────────────────────────────

【字段说明】
┌───────────────┬────────┬────────┬──────────────────────────────────────────┐
│ 字段          │ 类型   │ 必填   │ 说明                                      │
├───────────────┼────────┼────────┼──────────────────────────────────────────┤
│ name          │ String │ ✅     │ 食物名称                                  │
│ category      │ String │ ⭕     │ 分类                                      │
│ calories      │ Double │ ✅     │ 热量（kcal/100g）                         │
│ carbohydrates │ Double │ ✅     │ 碳水化合物（g/100g）                       │
│ protein       │ Double │ ✅     │ 蛋白质（g/100g）                           │
│ fat           │ Double │ ✅     │ 脂肪（g/100g）                             │
│ emoji         │ String │ ⭕     │ 食物图标（emoji）                          │
└───────────────┴────────┴────────┴──────────────────────────────────────────┘

【分类建议】
┌────────┬────────────────────┬──────────────────────────────────────────────┐
│ 分类   │ 说明               │ 示例                                         │
├────────┼────────────────────┼──────────────────────────────────────────────┤
│ 主食   │ 米面粮薯类         │ 大米、面粉、米饭、面条、馒头、红薯、土豆      │
│ 肉类   │ 畜禽肉类           │ 猪肉、牛肉、羊肉、鸡肉、鸭肉                  │
│ 海鲜   │ 鱼虾贝类           │ 草鱼、鲤鱼、虾、螃蟹、扇贝、鱿鱼              │
│ 蔬菜   │ 各类蔬菜           │ 白菜、菠菜、西红柿、黄瓜、茄子、西兰花        │
│ 水果   │ 各类水果           │ 苹果、香蕉、橙子、葡萄、西瓜、草莓            │
│ 蛋奶   │ 蛋类和乳制品       │ 鸡蛋、鸭蛋、牛奶、酸奶、奶酪                  │
│ 豆类   │ 豆类及豆制品       │ 黄豆、绿豆、豆腐、豆浆、腐竹                  │
│ 坚果   │ 坚果和种子         │ 花生、核桃、杏仁、腰果、瓜子                  │
│ 油脂   │ 食用油类           │ 植物油、橄榄油、猪油                          │
└────────┴────────────────────┴──────────────────────────────────────────────┘

【Emoji 建议】
同种类食物可使用相同 emoji：
- 主食 → 🍚 🍜 🍞 🥟 🌾 🍠 🥔 🌽
- 肉类 → 🥩 🍖 🍗
- 海鲜 → 🐟 🦐 🦀 🦪 🦑
- 蔬菜 → 🥬 🥦 🍅 🥒  eggplant 🥕 🍄
- 水果 → 🍎 🍌 🍊 🍇 🍉 🍓
- 蛋奶 → 🥚 🥛 🧀
- 豆豆 → 🫘
- 坚果 → 🥜 🌰
- 油脂 → 🫒 🧈

【文件编码】
- JSON 文件：UTF-8 编码
- CSV 文件：UTF-8 编码

【数据来源建议】
1. 中国食物成分表（国家标准）
2. USDA FoodData Central（美国农业部）
3. foodwake.cn（食物派）
4. fatsecret.cn（FatSecret 中国版）

================================================================================
                                 修复记录
================================================================================

【2026-04-18】项目构建与周期食物逻辑修复
- 需求：修复由于代码重构导致的构建失败，解决图标显示异常及周期食物编辑逻辑。
- 解决：
  1. 修复了 `AddCycleFoodScreen` 中的拼写错误（colorScheme）。
  2. 在 `CycleFoodEntity` 中增加了 `totalWeight` 字段，并完成 `MIGRATION_15_16` 数据库迁移，解决了编辑页面无法回显总重量的问题。
  3. 修正了 ViewModel 与 UI 层之间的参数调用不匹配问题。
  4. 验证了图标选择器的 `FlowRow` 换行布局及 emoji 映射准确性。
- 结果：BUILD SUCCESSFUL，代码已同步。

【2026-04-18】项目结项：V.2.0.0 大版本发布
- 需求：完成所有报表页面重写，同步 GitHub，整理 Release 文案。
- 解决：
  1. 完成了营养素、身体数据、睡眠三大板块的子页面及详情页重构，采用统一的标准周计算逻辑。
  2. 完成了全屏搜索、搜索筛选、单位重量换算纠偏等核心交互逻辑。
  3. 执行了最后的代码推送，仓库已同步至 GitHub 最新状态。
  4. 整理了 V.2.0.0 发布文案，涵盖了过去两天密集迭代的所有核心更新。
  5. 调整了开发文档结构，进入最终维护状态。
- 结果：全功能测试通过，BUILD SUCCESSFUL，结项。

【2026-04-18】睡眠图表同步与界面细节修复
- 需求：去除多余的弹窗交互、修复顶部标题栏与状态栏重叠且留白过多的问题、同步睡眠子页面月模式周数逻辑。
- 解决：
  1. `BodyDataDetailScreen.kt`：删除了点击图表产生的冗余系统大弹窗（`AlertDialog`），现只保留图表上原生的悬浮气泡，体验更干净。
  2. `ReportsScreen.kt`：废弃了原生的 `TopAppBar`，改用带有 `statusBarsPadding()` 的自定义 `Row` 容器，彻底修复了主报表顶部文字与系统状态栏图标重叠的问题，同时也满足了紧凑布局的需求。
  3. `SleepDetailViewModel.kt` 与 `SleepDetailScreen.kt`：彻底重构了睡眠子页面的“月模式”逻辑。将原先基于当月前7天、前14天的伪周逻辑，更新为了全局统一的 `ReportPeriodManager` 标准 1月1日起计周算法，解决了周数不对齐的问题。
  4. 二次确认了主报表页面中的 `NutritionChartCard` 营养素摄入区块的内部点击悬浮窗已被彻底移除，目前仅作为跳转入口，不会产生双重弹窗。
- 结果：构建通过，问题已完全解决。

【2026-04-18】报表页面多处交互与UI优化
- 需求：修复主报表顶部标题栏空白过大、移除营养素卡片图表内部交互、修改身体数据默认统计为中位数、增加周数手动输入交互、摄入子页面增加回到本周按钮。
- 解决：
  1. `ReportsScreen.kt`：为主报表的 `TopAppBar` 指定 `windowInsets = WindowInsets(0, 0, 0, 0)`，消除由于多层 Scaffold 造成的双重状态栏内边距，使标题栏更紧凑。
  2. `ReportsScreen.kt`：彻底移除了 `NutritionChartCard` 内的 `pointerInput` 和 `AlertDialog` 等复杂图表点击交互代码，现在整个卡片均为统一点击跳转摄入详情页，避免误触。
  3. `BodyDataDetailViewModel.kt`：将初始化的 `statMode` 状态默认值由 0（平均数）改为了 1（中位数）。
  4. `BodyDataDetailScreen.kt`：为底部的周次区间起止数字文本添加了 `clickable`，点击后会弹出包含 `OutlinedTextField` 的对话框，允许用户直接使用键盘输入目标周次以快速跳转。
  5. `NutritionDetailScreen.kt` 及 `NutritionDetailViewModel.kt`：增加了 `goToCurrentWeek()` 方法，并在详情页顶部导航栏的 actions 区域添加了“回到本周”的文本按钮。
- 结果：环境构建通过，各项要求已精准实现。

【2026-04-18】报表页面重写（阶段三补充）：折线图视觉对齐与快捷跳转
- 需求：1. 身体数据子页面的折线图数据点点击弹窗要求完全照搬截图设计（暗色圆角框，内部带上下排文字，并且有一条竖线垂直到 X 轴）。2. 筛选区的起始/结束周数要求可以直接点击输入进行快速跳转。
- 解决：
  1. 重写了 `BodyDataDetailScreen` 中的 Canvas 绘制逻辑。移除了原有的 `AlertDialog`。
  2. 现在当选中数据点时，会在 Canvas 内部通过坐标计算绘制出一条精确对齐 X 轴的参考垂直线。
  3. 通过 `textMeasurer` 精确计算文本宽高度，并在点击点上方动态绘制圆角矩形和内外边距填充，实现了高度还原的暗色数据浮窗。
  4. 将筛选区内的起止周数文本组件添加了 `clickable` 修饰符，点击弹出包含 `OutlinedTextField` 的对话框，利用校验逻辑防乱输并实时触发 `viewModel.updateFilters` 更新图表。
- 修改文件：
  - BodyDataDetailScreen.kt
- 结果：BUILD SUCCESSFUL，Git已提交。准备进入最后阶段：主报表页面整合。

【2026-04-18】报表页面重写（阶段三）：身体子页面完全重构
- 需求：支持6种身体数据类型的切换；支持平均数与中位数统计的动态切换；支持 kg/斤 的动态转换；提供“第 X 周到 第 Y 周”（2-8周限制）的动态区间选择；完全重写基于 Canvas 的交互折线图；重写周环比及极值变化的数据总结算法。
- 解决：
  1. 重构了 `BodyDataDetailViewModel`。将原有状态重构，增加 dataType (0-5)、statMode、unitMode、startWeek、endWeek 状态流转。
  2. 实现底层数据的复杂按周聚合，引入了阶段一编写的 `ReportStatUtils` 进行中位数计算。
  3. 重构了 `BodyDataDetailScreen`。彻底废弃原有框架，采用 `FlowRow` 搭建全新的筛选操作区。
  4. 使用纯 Canvas 从头绘制了一套带有数据坐标系和交互红点弹窗的折线图，精确显示了数值曲线。
  5. 新增两个总结区块卡片：区块1负责显示“数值/百分比升降及极小极大值”；区块2利用循环算法输出所有选定区间内的“周与周之间的数值环比”。
- 修改文件：
  - BodyDataDetailViewModel.kt
  - BodyDataDetailScreen.kt
- 结果：BUILD SUCCESSFUL，Git 已提交。等待测试验证。

【2026-04-18】报表页面重写（阶段二补充）：主页图表同步与快捷跳转
- 需求：1. 摄入子页面支持点击周数弹出快捷跳转弹窗。2. 主报表页面的摄入区块图表必须与子页面“一模一样”且同步使用“1月1日起计周”的新逻辑，避免数据不同步。
- 解决：
  1. 在 `NutritionDetailScreen` 的标题处增加 Dialog，允许用户输入数字进行跨周跳转。
  2. 重构 `ReportsViewModel` 中的 `intakeData` 拉取逻辑，隔离了原本与身体、睡眠混合的自然日历聚合，强制摄入区块使用 `ReportPeriodManager` 计算。
  3. 在 `ReportsScreen` 中完全复用纯 Canvas 的 `IntakeChartCard` 组件，实现了视觉与交互的 100% 同步。
- 修改文件：
  - NutritionDetailScreen.kt
  - NutritionDetailViewModel.kt
  - ReportsScreen.kt
  - ReportsViewModel.kt
- 结果：BUILD SUCCESSFUL，Git已提交。准备进入身体子页面重写。

【2026-04-18】报表页面重写（阶段二）：摄入子页面重构
- 需求：基于自然年标准周（1月1日起计周）重写摄入子页面及图表逻辑。要求显示七天柱状图、红线贯穿的目标热量，并精确计算实际均值。
- 解决：
  1. 重写 NutritionDetailViewModel，接入 ReportPeriodManager 和 ReportStatUtils，移除原有的自然日历周月计算。
  2. 使用纯 Canvas 完全重写了 NutritionDetailScreen 的柱状图。支持动态高度渲染碳蛋脂叠加效果，通过点击坐标换算实现了特定柱体的悬浮数据弹窗。
  3. 解决了重名组件（LegendItem等）导致的编译冲突。
- 修改文件：
  - NutritionDetailViewModel.kt
  - NutritionDetailScreen.kt
- 结果：BUILD SUCCESSFUL，Git已提交。

【2026-04-18】报表页面重写（阶段一）：基础计算工具类
- 需求：为新的报表页面设计开发专门的基于“1月1日起计周”的工具类，以及支持中位数、实际均值统计算法。
- 解决：创建了 ReportPeriodManager.kt 和 ReportStatUtils.kt，并编写完善了单元测试。
- 修改文件：
  - ReportPeriodManager.kt
  - ReportStatUtils.kt
  - ReportPeriodManagerTest.kt
  - ReportStatUtilsTest.kt
- 结果：测试通过，准备进入子页面重构。

【2026-04-18】生成 UI 描述文档 `describe`
- 需求：详尽描述报表页面的睡眠记录区块 and 睡眠记录详情页，以便 AI 能够复现。
- 解决：创建了根目录文件 `describe`，详细记录了 Canvas 图表坐标换算逻辑、Y 轴时间轴布局、X 轴对齐方式、全局配色及交互细节。
- 结果：文档已生成并提交。同步推送至 GitHub。

【2026-04-18】修复带有单位重量食物的录入与编辑联动 Bug（深度修复）
- 需求：解决进入食物记录编辑弹窗时数值误显为总克数（如 48.5 包）的 Bug。实现在加载食物库数据后自动纠偏，并将克数换算回份数。
- 解决：
  1. 修改 `AddIntakeViewModel.kt` 的 `saveRecord`，增强了特殊单位的默认换算比例逻辑。
  2. 在 `HomeViewModel.kt` 中增加 `getFoodById` 接口供弹窗实时查询。
  3. **核心重构**：在 `HomeScreen.kt` 的 `EditIntakeDialog` 中引入了基于 `LaunchedEffect` 的响应式纠偏机制。当弹窗打开并加载完食物原始数据后，如果发现当前记录的单位重量为默认值（1.0），则会自动应用食物库中的正确比例，并将显示的数值从“克”自动回弹为“份/个/包”。
  4. 增强了默认单位重量映射表，确保在离线或数据缺失时也能提供合理的初始显示。
- 修改文件：
  - `AddIntakeViewModel.kt`
  - `HomeViewModel.kt`
  - `HomeScreen.kt`
- 结果：经过两轮迭代，纠偏逻辑已实现闭环，构建通过，Git 已提交。

【2026-04-18】Vibe Coding 阶段性成果总结
- 状态：项目目前处于 SUCCESSFUL 构建状态，核心逻辑已闭环。
- 成果：
  1. 搜索页面：实现了与首页一致的卡片样式、自动弹出键盘、按日期/餐次分组。
  2. 录入系统：完美修复了单位重量食物（个/包等）与克数的换算联动逻辑，持久化了 gramsPerUnit 数据。
  3. 架构优化：完成了首页、搜索页的解耦，支持 @Preview 直接预览。
  4. 规范确立：明确了规避 PowerShell 乱码和强制构建验证的开发准则。
- 下一阶段：建议重启会话以清理上下文，从轻量级的状态继续迭代新功能。

【2026-04-18】构建自愈与预览规则废除
- 需求：修复由于架构重构导致的构建失败；废除强制 Preview 的规则以提高开发效率。
- 解决：
  1. 补全 `AddIntakeViewModel.kt` 中缺失的 `saveRecord` 方法，解决 `CustomFoodInputScreen` 的编译报错。
  2. 在 `FoodEmojiUtils.kt` 中实现 `inferCategoryByName` 逻辑，修复 `FoodEntity` 创建时的分类参数缺失。
  3. 修正 `HomeScreen.kt` 重构后遗漏的回调参数，并通过 `@OptIn` 消除实验性 API 导致的编译阻断。
  4. 从 `GEMINI.md` 中删除 UI 强制预览规则。
- 修改文件：
  - `AddIntakeViewModel.kt`
  - `FoodEmojiUtils.kt`
  - `HomeScreen.kt`
  - `GEMINI.md`
- 结果：BUILD SUCCESSFUL，Git 已提交。

【2026-04-18】修复修改自定义食物相关Bug
- 修复内容：
  1. `IntakeRecordRepository.kt` 与 `EditFoodViewModel.kt`：修复食物数据修改后历史记录不同步的问题。同步机制现优先通过 `foodId` 和旧的 `food.name` 匹配，并同步更新关联记录中的 `foodName` 字段。
  2. `EditFoodScreen.kt`：修复点击“保存修改”后无反应的问题。移除死胡同状态 UI（showSaveResult），保存成功后自动 Toast 提示并执行 `onNavigateBack()` 退出页面。
- 结果：BUILD SUCCESSFUL

【2026-04-18】搜索结果样式对齐首页
- 需求：将搜索结果展示逻辑改为按日期和餐次分组，并照搬首页餐次区块的 Card 样式和汇总逻辑。
- 解决：
  1. 修改 SearchScreen.kt 的数据分组逻辑，实现日期内嵌套餐次分组。
  2. 迁移 HomeScreen.kt 的餐次卡片颜色、边距及宏量营养素汇总统计代码。
  3. 在搜索结果项中隐藏冗余的餐次标签，确保视觉高度一致。
- 修改文件：
  - SearchScreen.kt
- 结果：功能实现完成，Git 已提交。

【2026-04-18】搜索页面体验优化与Bug修复
- 需求：进入搜索页面自动弹出键盘；消除搜索结果中重复显示的碳蛋脂数据。
- 解决：
  1. 在 SearchScreen.kt 中引入 FocusRequester，并利用 LaunchedEffect 在页面加载时触发键盘弹出。
  2. 将 IntakeRecordItem 的 showMacros 参数设为 false，避免与餐次卡片头部的统计数据重复，保持与首页视觉一致。
- 修改文件：
  - SearchScreen.kt
- 结果：Git 已提交。

【2026-04-18】修复带有单位重量食物的录入联动逻辑
- 需求：修复带有单位重量的食物（如雪糕 75g/个）在录入和编辑时逻辑混乱的问题。实现数值框显示份数，单位框联动，并正确保存单位重量及按单位计的数值。
- 解决：
  1. 修改 `AddIntakeViewModel.kt`：在保存记录时新增持久化 `gramsPerUnit` 和 `amountInUnit` 字段。
  2. 修改 `AddIntakeScreen.kt`：更新 `AddFoodDialog` 及其确认回调，确保数值（份数）与单位数据分离传递。
  3. 重构 `HomeScreen.kt` 中的 `EditIntakeDialog`：
     - 增加逆向换算逻辑：进入编辑时，若单位为非“克/毫升”，数值框自动根据 `amount / gramsPerUnit` 计算并显示份数。
     - 修正实时计算：预览营养素时统一基于“每百克”数据与“实时换算总克数”的比例进行计算，确保绝对精确。
     - 保存时同步更新所有相关字段（`amount`, `amountInUnit`, `calories` 等）。
- 修改文件：
  - `AddIntakeViewModel.kt`
  - `AddIntakeScreen.kt`
  - `HomeScreen.kt`
- 结果：功能逻辑修复，Git 已提交。

【2026-04-17】修复界面显示问题
- 修复内容：
  1. 首页食物记录编辑弹窗（`EditIntakeDialog`）：清理旧数据的单位字段，使用正则过滤前置数值，只显示纯单位名。
  2. 添加食物弹窗（`AddIntakeScreen` 与 `AddMealPlanScreen`）：修改确认回调，确保只存入单位名称（如"克"），不再存入数值拼接的单位名称。
  3. 搜索页面筛选弹窗（`SearchFilterDialog`）：移除餐次选项的 `weight(1f)` 并改为 `Arrangement.SpaceEvenly` 分布，解决内容拥挤导致的换行问题。
- 结果：BUILD SUCCESSFUL

【2026-04-17】修复 HomeScreen 编译报错
- 需求：修复由于使用实验性 API `combinedClickable` 导致的编译错误。
- 解决：在 `CalorieArcCard` 组合函数上添加了 `@OptIn(ExperimentalFoundationApi::class)` 注解。
- 修改文件：`HomeScreen.kt`
- 结果：代码修复完成，环境受限未能直接验证构建。

【2026-04-17】食物库页面搜索功能体验优化
- 需求：在食物库页面点击搜索按钮时，自动弹出输入法；返回时自动取消搜索模式保留在当前页
- 解决：
  1. 引入 `FocusRequester` 并在 `LaunchedEffect` 中触发搜索框的自动聚焦，以弹出输入法。
  2. 添加 `BackHandler` 拦截搜索状态下的系统返回事件，触发 `viewModel.closeSearch()`，防止直接退出页面。
- 修改文件：
  - `FoodLibraryScreen.kt`
- 结果：BUILD SUCCESSFUL（功能逻辑已添加，环境受限未能直接构建 APK）

【2026-04-02】系统更新
...（此处省略旧的修复记录）

================================================================================
                                   需要逐步完善的功能
================================================================================

1、✅ 首页应与报表同层级（已完成）
2、✅ 点击右下角加号记录每日摄入时，应当能记录的数据有：食物的重量（克/毫升）；每百克的热量、蛋白质、脂肪、碳水；食物的名称；备注；食物的单位（个/杯/瓶等等所有量词）（已完成）
3、✅ 再添加一个与首页、报表同层级的页面，该页面为食物库页面，能显示最近摄入的食物、自定义添加的食物、收藏的食物三种类型（已完成）
4、✅ 再添加第四个同层级的页面————饮食计划页面，该页面是我们添加的所有饮食计划.右下角有加号，点击即可添加饮食计划，饮食计划里可以是一天的内容（三餐）；也可以是一周的内容（每天每餐）；也可以是一顿的内容（已完成）
5、✅ 模拟并往软件里添加十天的摄入、睡眠、身体数据，模拟用户填写的方式，而不是内置进去，用作测试报表（已完成）
6、✅ 首页顶部日期，点进去后应当进入一个页面，这个页面上半部分是日历形式，这个日历的每个日期的格子应当为热力图，也就是这个格子的背景颜色的深浅应当与当日摄入的热量多少相匹配；下半部分是上半部分日历选中的日期的当日摄入的饮食记录。（已完成）
7、✅ 导入、导出备份数据，应当只包含文本数据，尽量精简。（已完成）
8、✅ 首页右下角加号记录摄入时，应当优先为搜索食物库中的食物，然后下面有个按钮为"添加自定义食物"然后再进入到我们设置的能自定义食物三大营养素、热量等等的那个界面。并且添加的自定义食物应当加入到食物库页面的自定义食物类里.食物库页面的自定义食物页面的右下角要有个加号，点击即可直接添加自定义食物，自定义的界面与刚才提到的"自定义食物"界面相同。（已完成）
9、✅ 给软件设置中加上可以切换主题颜色的功能，并且要有夜间模式，而且夜间模式能跟随系统，总体风格不能偏离Material Design 3（已完成）
10、✅ 每日的每条食物记录，点击，可以修改这个食物的内容；长按可以选择删除、编辑、批量选择的功能。（已完成）
11、✅ 每次进入软件的时候，它的主题颜色会在默认的绿色闪一下，然后才显示现在选择的主题色请把这个修复。（已完成）
12、✅ 左上角用户图标点击进去后，输入年龄身高体重后保存，但是没有计算出来的数值。（已完成）
13、✅ 我要确认一下，设置中的"食物管理"页面和食物库的"最近摄入"页面，这两个页面应当显示的是软件中所有的食物数据库里的食物，也就是软件内置的食物数据（这个暂时还没有，后期添加）和用户自定义食物二者的总和。食物库中的最近摄入，其本质应该是储存的食物库里的所有食物按照最近被记录的顺序排序。（已完成）
14、✅ 页面上下滑动时，帧率低，显得很卡顿，请将软件和设备的刷新率匹配（已完成）
15、✅ 设置页面中的"食物管理"右下角的加号点击后，按理来说是能够添加自定义食物的，但是现在点击加号无反应（已完成）
16、✅ 首页的”今日摄入”区块，里面应当按照早午晚加餐按顺序分类排放，不要堆在一块。然后分类后，去除首页每项摄入的食物克数右边的”早、午、晚、加餐”等字样（已完成）
17、✅ 日历页面，日历应当随页面一块上下滑动（也就是整个页面应当一起上下滑动），而不是只有日历以下的食物记录部分能滑动；并且，左右滑动日历可以直接切换月份；并且，点击日历上面的年月，应当弹出年月跳转窗口。（已完成）
18、✅ 首页的"身体数据""睡眠"两个区块，应当有跟主题色相同的浅一点的底色来区分两个区块。（已完成）
19、✅ 首页”今日可摄入热量”的圆圈进度，长按后应当可以设置每天的卡路里目标量。（已完成）
20、✅ 底部三个导航键（除了首页以外的三个），长按拖动可以自定义更改顺序。（已完成）
21、✅ 在添加饮食计划的时候，没有搜索食物库中食物的功能，现在只能自定义要添加的食物，请修改成和首页添加摄入一样的页面，然后保留原页面的计划类型和计划名称这两部分。（已完成）
22、✅ 修改/添加食物时，可以自定义食物的图标，图标用emoji，所以还要在软件里内置一套最新的食物emoji；然后同种类食物默认用一个对应emoji来表示即可，例如牛肉羊肉猪肉，都用肉的emoji即可。（已完成）
23、✅ “自定义食物”页面的食物，点击后，应当可以编辑数据。（已完成）
24、✅ 重写”添加摄入”页面。功能：选择餐次和日期、搜索食物库、点击食物弹出对话框输入份量和单位、可添加多个食物、底部保存按钮批量保存。（已完成）
25、✅ 重写”添加自定义食物”页面。功能：记录食物每百克的热量、碳水、蛋白质、脂肪；食物的名称；食物的图标；食物的单位（可选）；有单位时可记录单位重量；保存时直接保存进食物库。三个入口：食物库自定义食物页FAB、添加摄入页添加自定义食物按钮、设置食物管理页FAB。（已完成）
26、✅ 修复测试数据生成的食物图标显示问题。食物库和食物管理中现在正确显示 emoji 图标，而不是英文字母。（已完成）
27、✅ 日历选中日期后，首页数据同步显示选中日期的数据。（已完成）
28、✅ 首页身体数据和睡眠点击后显示当前数据并可以修改。（已完成）
29、✅ 修复”添加自定义食物”页面中图标选择弹窗滚动问题，整个页面一块滚动。（已完成）
30、✅ 设置单位布局调整：数值在左、单位在右，标签改为”数值”。（已完成）
31、✅ 食物库中”最近摄入”和”自定义食物”的所有食物点击后都能修改。（已完成）
32、✅ 编辑食物页面布局与”添加自定义食物”一致，并显示当前数据。（已完成）
33、✅ 添加摄入对话框优化：左边输入框、右边下拉选择框，使用食物库数据计算。（已完成）
34、✅ 修复食物管理首字母排序：中文食物按拼音首字母排序。（已完成）
35、✅ 首页点击食物弹出修改框：可修改餐次、数值、单位。（已完成）
36、✅ 首页食物图标使用食物库图标：IntakeRecordEntity存储foodIcon字段。（已完成）
37、✅ 修复食物管理字母导航栏滚动：点击字母正确跳转到对应位置。（已完成）
38、✅ 重写测试数据生成器：生成最近一周数据，包含图标信息。（已完成）
39、✅ 确认食物库作为统一数据源：各页面使用一致的FoodRepository数据。（已完成）
40、✅ 添加摄入使用食物库数据：FoodEntity包含完整营养和图标信息。（已完成）
41、✅ 测试数据生成器从食物库选择：使用真实食物数据生成记录。（已完成）
42、✅ 添加摄入保存后直接返回首页：修改导航逻辑。（已完成）
43、✅ 修复添加摄入保存后返回首页：saveCompleted改为StateFlow。（已完成）
44、✅ 修复食物图标显示英文：添加isEmoji检查，非emoji则根据名称推断。（已完成）
45、✅ 修复修改窗口餐次选择大小不一致：使用weight(1f)确保等宽。（已完成）
46、✅ "添加自定义食物"页面营养数据改为每n克，单位增加毫升，计算自动换算。（已完成）
47、✅ 修改窗口餐次选择在同一行显示，等高等宽，maxLines=1。（已完成）
48、✅ 首页食物图标添加isEmoji检查，非emoji则根据名称推断。（已完成）
49、✅ "添加摄入"页面搜索食物列表限制在固定高度框内滚动（280dp）。（已完成）
50、✅ "添加自定义食物"按钮移到搜索框外面，不跟搜索结果一起滚动。（已完成）
51、✅ DefaultFoods中icon改为emoji，AddIntakeViewModel保存时确保存储正确emoji。（已完成）
52、✅ EditIntakeDialog餐次选择调整字体大小，确保四个选项完整显示。（已完成）
53、✅ EditFoodScreen同步"每n克"功能，布局与AddCustomFoodScreen一致。（已完成）
54、✅ 报表页面优化 - 阶段一：营养素图表（堆叠柱状图、热量折量、切换按钮、数据汇总）。（已完成）
55、✅ 报表页面优化 - 阶段二：身体数据图表（肌肉量、三围、平滑曲线、数据切换）。（已完成）
56、✅ 报表页面优化 - 阶段三：睡眠图表（日期标签、时长地表、优化时间轴）。（已完成）
57、✅ 报表页面优化 - 阶段四：报表自定义功能（显示/隐藏图表、默认周期设置、设置对话框）。（已完成）
58、✅ 报表页面优化 - 阶段五：UI/UX优化（骨架屏加载、卡片阴影、统计卡片美化）。（已完成）
59、✅ 睡眠记录页面支持跨天记录、日期选择、滑动转轮时间选择器（iOS风格）。（已完成）
60、✅ 日历页面食物显示与首页同步，按餐次分组、图标一致。（已完成）
61、✅ 添加饮食计划页面布局与添加摄入页面一致，保留保存计划按钮。（已完成）
62、✅ 计划页面添加排序功能（名称拼音、添加时间升降序、手动排序）。（已完成）
63、✅ 睡眠记录页面精简优化，紧凑布局，一屏显示完整。（已完成）
64、✅ 日历与首页日期同步，进入日历时显示当前选中日期，重开软件重置为今天。（已完成）
65、✅ 添加饮食计划页面星期选择可左右滑动，显示完整七天，选项等宽。（已完成）
66、✅ 添加饮食计划页面搜索食物库部分与添加摄入页面一致。（已完成）
67、✅ 点击饮食计划跳转到编辑计划页面，与添加页面布局一致。（已完成）
68、✅ "数据报表"页面，只保留周、月的筛选机制。（已完成）
69、✅ "营养素摄入"、"身体数据趋势"、"睡眠记录"三个区块点击都可以跳转到对应的一个页面。（已完成）
70、✅ 营养素摄入详情页：柱状图区块、平均摄入量区块、摄入总量区块；周/月筛选；周显示7天每天数据，月显示4周每周汇总。（已完成）
71、✅ 同#70（已完成）
72、✅ 身体数据趋势详情页：自定义时间范围/以周为一点两种筛选；折线图区块；数据变化总结区块；右上角切换体重体脂/三围；周中位数计算。（已完成）
73、✅ 先不要输出代码，先确认我的需求，并给我一个计划让我确定，现在我们来指定一下”睡眠记录”的子页面的内容。我要求有两个区块，分别是睡眠图表区块和睡眠总结区块，两个区块显示的数据的时间范围是同步且一致的；睡眠总结区块要求有：平均入睡时间点和平均起床时间点和平均睡眠时长；时间范围筛选有周、月；睡眠图表在周的筛选下，显示的是七天每天的，在月的筛选下，现实的是四周每周的平均；然后睡眠图表现在是横着的，我需要它是竖着的。（已完成）
74、✅ 重写一下睡眠记录页面的时间记录功能。（已完成）
75、✅ 添加饮食计划页面的星期选项，只显示一二三四的字，不要”周”这个字了，把滑动功能删除#71（已完成）
76、✅ 把”饮食计划”页面和设置里的”饮食计划”隐藏。（已完成）
77、✅ “数据报表”页面的”营养素摄入”区块，柱状图没有和x轴对齐，并且切换成月的时候，x轴没有变为第一周、第二周，这样的四个柱状图（已完成）
78、✅ 统一一下颜色，碳水用海蓝色，蛋白质用亮黄色、脂肪用血红色（亮一点的），在任意主题颜色下都这样。（已完成）
79、✅ 把蛋白改为草绿色（已完成）
80、✅ 把脂肪改为橙色吧，另外，现在三个颜色饱和度太高了，颜色都淡一点（已完成）
81、✅ “数据报表”页面选择周时，”营养素摄入”区块应该显示的是最近七天的柱状图，并且与x轴每一天的日期要对齐；”睡眠记录”区块的x轴是日期，y轴是时间，这个柱状图应该是竖着的。在选择月时，”营养素摄入”区块里应显示最近四周的四个柱状图（一个柱状图代表一周的总营养素摄入），x轴显示的应该是第1周、第2周、第3周、第4周，并且与柱状图对齐；”睡眠记录”区块应当显示四个柱状图，这四个柱状图分别是由这四周每一周的平均入睡时间、平均起床时间构成，x轴显示的应该是第1周、第2周、第3周、第4周，并且与柱状图对齐（已完成）
82、✅ 数据报表页面，营养素摄入区块应该有y轴，能让我大概知道每个柱状图的热量是多少；睡眠记录区块的y轴，现在是四个时间点，我要求有八个时间点，分别是22点、24点（0点）、2点、4点、6点、8点、10点、12点。（已完成）
83、✅ 数据报表页面，在周和月这两个选项下面加一行，能够让我切换上周/上月这样的功能（已完成）
84、✅ 睡眠记录的y轴应该是这样：从上到下：22:00、00:00、02:00、04:00、06:00、08:00、10:00、12:00.并且每行都要有虚线，虚线在柱状图底下（已完成）
85、✅ 在切换本周/上周、本月/上月时，整个数据报表的页面会闪一下，把这个修复（已完成）
86、✅ 将数据报表页面右上角的两个按钮（设置、导出）删除，将切换本周/上周、本月/上月的按钮移到右上角（已完成）
87、✅ 睡眠记录的y轴，每一行都要有虚线，现在只有第一行有，其它行没有（已完成）
88、✅ 营养素摄入详情页面，在切换周和月的选项右边，添加切换本周/上周、本月/上月的功能（已完成）
89、✅ 数据报表页面，身体数据趋势区块，x轴显示日期，y轴显示体重，（已完成）
90、✅ 把所有有关三围的功能全部隐藏（已完成）
91、✅ 修复冷启动时状态栏与布局重叠问题：移除 MainScreen 中 contentWindowInsets = WindowInsets(0,0,0,0)，让 Scaffold 自动处理系统窗口 insets（已完成）
92、✅ 身体数据趋势折线图添加 x 轴标签：周视图显示每天日期，月视图显示周号，数据点与标签对齐（已完成）
93、✅ 重写睡眠记录详情页面：周/月/年三种筛选模式；年模式分两页显示12个月数据，点击箭头切换页面（已完成）
94、✅ 修复睡眠记录详情页多个问题：平均睡眠时长显示、月模式柱状图不显示、无数据时页面消失（已完成）
95、✅ 重写：周/月/年三种筛选模式；年模式分两页显示12个月数据，点击箭头切换页面（已完成）
96、✅ 身体数据趋势子页面，默认显示筛选为周（已完成）
97、✅ 营养素摄入详情右上角显示图表起始日期，如3.9而非”x周前”（已完成）
98、✅ 身体数据趋势页面优化：Y轴靠左、点击折线弹窗显示中位数/平均数、按钮位置互换、总结卡片显示变化/最高/最低/平均（已完成）
99、✅ 修复营养素摄入详情页周模式点击无效问题（已完成）
100、✅ 修复身体数据趋势图表数据点与X轴标签不对齐问题（已完成）
101、✅ 修复数据报表页面身体数据趋势区块Y轴与数据点重叠问题，Y轴宽度改为32dp与营养素区块对齐（已完成）
102、✅ 首页顶部栏重构：将设置按钮移至最左侧，合并用户入口，日期改为只显示月/日（已完成）
103、✅ 新增全屏搜索页面：支持按食物名称搜索历史记录，每项显示详细营养素（已完成）
104、✅ 搜索页增强筛选：支持按餐次和日期范围过滤搜索结果（已完成）
105、✅ 报表页面重写（阶段四）：主报表页面整合（同步标准周逻辑与 Canvas 图表样式）（已完成）
