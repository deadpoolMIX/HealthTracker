package com.example.healthtracker.data.local.database

import com.example.healthtracker.data.local.entity.FoodEntity

/**
 * 内置食品营养数据
 * 现已改为用户自行导入数据，此处保留空列表作为占位
 */
object DefaultFoods {
    val foods: List<FoodEntity> = emptyList()
}