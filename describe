# HealthTracker 睡眠报表 UI 描述指南

本文档旨在通过详尽的视觉与逻辑描述，指导 AI 100% 还原 HealthTracker 应用中的“睡眠报表区块”和“睡眠记录详情页”。

---

## 1. 报表页面：睡眠记录区块 (SleepChartCard)

### 1.1 容器结构
- **卡片容器**：使用 `MaterialTheme.colorScheme.surface` 背景，圆角 `16.dp`，阴影高度 `2.dp`。
- **内边距**：四周统一 `16.dp`。
- **整体布局**：垂直 `Column`。

### 1.2 头部信息
- **标题**：左侧显示“睡眠记录”，字体 `titleMedium`，加粗。
- **汇总区块**：三个水平分布的统计项（StatItem），背景为 `surfaceVariant.copy(alpha = 0.3f)`，圆角 `8.dp`。
    - **平均入睡**：显示时间（如 23:30）。
    - **平均起床**：显示时间（如 07:15）。
    - **平均时长**：显示时长（如 7h45m）。
- **字体样式**：数值为 `primary` 颜色、加粗、`titleMedium`；标签为 `onSurfaceVariant`、`labelSmall`。

### 1.3 核心图表 (Canvas 实现)
- **Y 轴 (时间轴)**：
    - **位置**：图表左侧，宽度约 `40.dp`。
    - **刻度范围**：从上到下固定为 `22:00`, `00:00`, `02:00`, `04:00`, `06:00`, `08:00`, `10:00`, `12:00`。
    - **总跨度**：14 小时。
- **网格背景**：在每个时间刻度位置绘制横向虚线，颜色为 `onSurfaceVariant`，虚线样式 `dashPathEffect(floatArrayOf(4f, 4f))`。
- **X 轴 (日期/周期)**：
    - **周模式**：固定 7 个槽位。标签为 `M/D` 格式（如 4/18）。
    - **月模式**：4 个槽位（第1周...第4周）。
- **睡眠柱状图**：
    - **表现形式**：垂直悬浮条形。
    - **计算逻辑**：
        - 顶部 Y 坐标 = `(入睡时间点 - 22:00) / 14小时 * 总高度`。
        - 底部 Y 坐标 = `(起床时间点 - 22:00) / 14小时 * 总高度`。
    - **视觉样式**：颜色为 `primary.copy(alpha = 0.8f)`，圆角 `4.dp`。

---

## 2. 睡眠记录详情页 (SleepDetailScreen)

### 2.1 顶部导航栏 (TopAppBar)
- **标题**：“睡眠记录详情”，字体 `Medium`。
- **左侧**：返回箭头图标。

### 2.2 周期切换器 (PeriodSelectorWithArrows)
- **容器**：圆角 `12.dp` 的 `Card`。
- **布局**：水平 `Row`，两端对齐。
- **左/右箭头**：`IconButton`，图标分别为 `KeyboardArrowLeft/Right`。
- **中间切换**：三个 `FilterChip`（周、月、年），选中的背景为 `primary`。

### 2.3 睡眠总结卡片 (SleepSummaryCard)
- **视觉重心**：三个大号数值。
- **布局**：Row 内部三个 `SummaryItem`，中间用垂直分隔线（高度 `48.dp`, 宽度 `1.dp`, 颜色 `outlineVariant`）分开。
- **数值**：颜色为 `primary`，字体 `headlineSmall` (约 `24.sp`)，加粗。

### 2.4 详情图表区块 (YearSleepChart / MonthSleepChart)
- **年模式特殊交互**：
    - 顶部有页码指示器 `1/2`。
    - 点击左右箭头切换“前半年”和“后半年”。
    - 每页显示 6 个月的柱状图。
- **点击交互 (AlertDialog)**：
    - 点击图表中的任何一根柱子（通过 `detectTapGestures` 命中测试），弹出详情对话框。
    - **对话框内容**：显示该日/周/月的具体入睡、起床、时长和记录天数。
    - **样式**：Label 在左（灰色），Value 在右（主色、加粗）。

---

## 3. 全局视觉规范

- **配色方案**：
    - **主色 (Primary)**：当前主题色。
    - **辅助色 (onSurfaceVariant)**：用于辅助线和标签。
- **时间换算公式 (核心逻辑)**：
    ```kotlin
    fun convertToChartHour(h: Float): Float {
        return if (h < 12f) h + 24f else h // 跨天处理：01:00 转为 25:00
    }
    // 图表起始: 22f (22:00), 结束: 36f (次日12:00)
    ```
- **柱子对齐**：所有模式下，X 轴标签必须与 Canvas 中绘制的柱子中心对齐。
